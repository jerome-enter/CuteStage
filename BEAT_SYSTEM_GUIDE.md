# Beat 시스템 가이드

## 📚 목차

1. [Beat란 무엇인가?](#beat란-무엇인가)
2. [왜 Beat 시스템인가?](#왜-beat-시스템인가)
3. [Beat의 구조](#beat의-구조)
4. [사용 방법](#사용-방법)
5. [템플릿 활용](#템플릿-활용)
6. [기술적 구현](#기술적-구현)

---

## Beat란 무엇인가?

**Beat**(비트)는 연극에서 사용되는 용어로, **극적 의미의 최소 단위**를 뜻합니다.

### 연극 용어로서의 Beat

> "A beat is the smallest unit of action in a play. It contains one main action (objective) that a
character pursues."
>
> \- Konstantin Stanislavski (연극 이론가)

연극 연출에서 Beat는 다음과 같이 정의됩니다:

- **하나의 의도(Objective)**: "나는 상대방을 설득하고 싶다"
- **명확한 행동(Action)**: 다가가기, 손 흔들기, 소리 지르기 등
- **감정의 변화**: 기대 → 실망, 분노 → 안도 등

### CuteStage에서의 Beat

우리 앱에서 Beat는 다음을 하나의 패키지로 묶은 것입니다:

```
Beat = 캐릭터 동작 + 감정 + 대사 + 배경 + 조명 + 음향
```

**예시: "첫 만남" Beat**

```kotlin
Beat(
    name = "첫 만남",
    description = "두 사람이 처음 만나는 순간",
    duration = 4초,
    layers = {
        캐릭터A: 왼쪽에서 등장 → 중앙으로 이동, 기대감, "안녕하세요!"
        캐릭터B: 오른쪽에서 등장 → 중앙으로 이동, 설렘, "반갑습니다!"
        배경: 공원
        조명: 밝음
    }
)
```

---

## 왜 Beat 시스템인가?

### 1. **의미 중심 작업**

❌ **나쁜 예: 동작만 나열**

```
1. 캐릭터A 걷기
2. 캐릭터A 멈추기
3. 캐릭터A 손 흔들기
4. 캐릭터B 등장
```

→ **문제점**: "왜 이렇게 움직이지?" 맥락이 없어서 어색함

✅ **좋은 예: Beat로 구성**

```
Beat 1: "첫 만남" - 두 사람이 서로를 발견하고 반가워하는 순간
Beat 2: "어색한 침묵" - 무슨 말을 해야 할지 몰라 긴장하는 순간
Beat 3: "대화 시작" - 용기를 내어 말을 거는 순간
```

→ **장점**: 각 Beat가 명확한 의미를 가지고, 자연스럽게 이어짐

### 2. **초보자 친화적**

연극 제작 경험이 없어도:

- "첫 만남", "갈등", "화해" 같은 **익숙한 상황**으로 생각
- 템플릿을 선택하여 **빠르게 프로토타입 제작**
- 세부 동작은 시스템이 자동으로 처리

### 3. **프로페셔널에게도 유용**

- **블로킹(Blocking) 테스트**: "이 장면에서 A가 여기 서고, B가 저기..."
- **타이밍 실험**: "이 Beat는 3초가 맞나? 5초로 늘려볼까?"
- **연출 커뮤니케이션**: "이 Beat에서 느낌은 '긴장'이야"

### 4. **협업에 최적화**

```
연출가: "첫 만남 → 어색한 침묵 → 고백"
작가: "고백 후에 '당황' Beat 추가하면 어때요?"
배우: "고백 Beat에서 제가 왼쪽에 서있는 게 나을 것 같아요"
```

---

## Beat의 구조

### 레이어드 아키텍처

하나의 Beat는 여러 레이어가 동시에 진행됩니다:

```
┌─────────────── Beat: "대립" (5초) ───────────────┐
│                                                    │
│  📍 캐릭터 레이어                                  │
│    캐릭터A: 빠르게 다가가기, 화남, "왜 그랬어?"    │
│    캐릭터B: 물러서기, 당황, "내가 그럴 수밖에..."  │
│                                                    │
│  🎨 배경 레이어                                    │
│    배경: 거리                                      │
│                                                    │
│  💡 조명 레이어                                    │
│    밝기: 70% (조금 어둡게)                         │
│    색: 차가운 톤                                   │
│                                                    │
│  🔊 음향 레이어                                    │
│    BGM: 긴장감 있는 음악                           │
│    효과음: 발소리                                  │
│                                                    │
└────────────────────────────────────────────────────┘
```

### 데이터 모델

```kotlin
data class Beat(
    val id: String,
    val name: String,              // "대립"
    val description: String,       // "두 사람이 대립하는 긴장된 순간"
    val duration: Float,           // 5.0 (초)
    val layers: BeatLayers         // 모든 레이어
)

data class BeatLayers(
    val characters: List<CharacterAction>,  // 캐릭터들
    val background: BackgroundLayer?,       // 배경
    val lighting: LightingLayer?,           // 조명
    val sound: SoundLayer?,                 // 음향
    val dialogues: List<DialogueAction>     // 대사
)
```

---

## 사용 방법

### 1. **앱에서 Beat 시나리오 만들기**

#### Step 1: 캐릭터 추가

```
👥 등장인물
[+] 추가 버튼 클릭
  → 이름: "철수", 성별: 남성
  → 이름: "영희", 성별: 여성
```

#### Step 2: Beat 템플릿 선택

```
🎬 타임라인 (현재 비어있음)

──────────────────────────────────

📖 Beat 템플릿 팔레트
[만남] [갈등] [감정] [작별] [단독]

만남 탭:
  ├─ 👋 첫 만남
  │   두 사람이 처음 만나는 순간
  │   👥 2명  ⏱️ 4초  💬 2개
  │
  ├─ 😅 어색한 침묵
  │   서로를 의식하며 어색한 분위기
  │   👥 2명  ⏱️ 3초
```

#### Step 3: 타임라인 구성

```
🎬 타임라인
┌──────┐ ┌──────┐ ┌──────┐
│  #1  │ │  #2  │ │  #3  │
│첫만남│ │어색함│ │ 고백 │
│ 4초  │ │ 3초  │ │ 5초  │
└──────┘ └──────┘ └──────┘

총 3개 비트 (약 12초)
```

#### Step 4: 저장 및 재생

```
[💾 저장]
  → 제목: "공원에서의 첫 만남"
  → 설명: "철수와 영희의 운명적인 만남"
  
[▶️ 재생]
  → StageView에서 실행
  → 캐릭터들이 움직이고 대사 출력
```

### 2. **코드로 Beat 생성하기**

```kotlin
// 템플릿 사용
val beat = BeatTemplates.firstMeeting(
    characterA = CharacterInfo("char1", "철수", CharacterGender.MALE),
    characterB = CharacterInfo("char2", "영희", CharacterGender.FEMALE),
    greetingA = "어... 안녕하세요!",
    greetingB = "반가워요!"
)

// 커스텀 Beat
val customBeat = Beat(
    id = "custom_1",
    name = "놀라운 발견",
    description = "철수가 중요한 것을 발견하는 순간",
    duration = 4f,
    layers = BeatLayers(
        characters = listOf(
            CharacterAction(
                characterId = "char1",
                characterName = "철수",
                gender = CharacterGender.MALE,
                movement = Movement(
                    type = MovementType.STAY,
                    from = Position.CENTER
                ),
                emotion = Emotion(EmotionType.SURPRISED, 0.9f),
                facingDirection = Direction.FORWARD
            )
        ),
        dialogues = listOf(
            DialogueAction(
                characterId = "char1",
                text = "이게... 이게 바로 그거잖아!",
                emotion = EmotionType.EXCITED,
                delay = 1.5f
            )
        )
    )
)
```

### 3. **Beat → TheaterScript 변환**

```kotlin
val beats = listOf(beat1, beat2, beat3)
val script = beats.toTheaterScript()

// StageView에서 재생
StageView(script = script)
```

---

## 템플릿 활용

### 기본 제공 템플릿

#### 1. 만남 카테고리 👋

- **첫 만남**: 양쪽에서 등장하여 중앙에서 만남
- **어색한 침묵**: 서로를 의식하며 긴장

#### 2. 갈등 카테고리 ⚡

- **대립**: 한 명이 다가가고 한 명이 물러남
- **물러서기**: 당황하여 뒤로 물러남

#### 3. 감정 카테고리 ❤️

- **고백**: 용기 내어 고백하는 순간
- **환호**: 기쁨을 표현하며 박수

#### 4. 작별 카테고리 👋

- **작별 인사**: 양쪽으로 퇴장하며 작별

#### 5. 단독 카테고리 🎭

- **독백**: 혼자 생각에 잠김
- **등장**: 무대에 등장

### 템플릿 커스터마이징

```kotlin
// 템플릿 가져오기
val template = BeatTemplates.firstMeeting(charA, charB)

// 대사 수정
val customized = template.copy(
    layers = template.layers.copy(
        dialogues = listOf(
            DialogueAction(charA.id, "오랜만이야!", EmotionType.EXCITED),
            DialogueAction(charB.id, "정말 오랜만이네!", EmotionType.HAPPY)
        )
    )
)

// 지속 시간 조정
val longer = customized.copy(duration = 6f)
```

---

## 기술적 구현

### 아키텍처

```
사용자 입력 (BeatCreatorScreen)
    ↓
Beat 리스트 생성
    ↓
BeatConverter
    ↓
TheaterScript (기존 포맷)
    ↓
StageView (재생)
```

### 핵심 클래스

#### 1. `Beat` - 비트 데이터

```kotlin
// app/src/main/java/com/example/cutestage/stage/beat/BeatModels.kt
data class Beat(
    val id: String,
    val name: String,
    val description: String,
    val duration: Float,
    val layers: BeatLayers
)
```

#### 2. `BeatConverter` - Beat → TheaterScript 변환

```kotlin
// app/src/main/java/com/example/cutestage/stage/beat/BeatConverter.kt
object BeatConverter {
    fun beatsToTheaterScript(beats: List<Beat>): TheaterScript
}
```

#### 3. `BeatTemplates` - 미리 정의된 템플릿

```kotlin
// app/src/main/java/com/example/cutestage/stage/beat/BeatTemplates.kt
object BeatTemplates {
    fun firstMeeting(...): Beat
    fun confrontation(...): Beat
    fun confession(...): Beat
    // ...
}
```

#### 4. `BeatCreatorScreen` - UI

```kotlin
// app/src/main/java/com/example/cutestage/ui/creator/BeatCreatorScreen.kt
@Composable
fun BeatCreatorScreen(...)
```

#### 5. `BeatCreatorViewModel` - 로직

```kotlin
// app/src/main/java/com/example/cutestage/ui/creator/BeatCreatorViewModel.kt
class BeatCreatorViewModel
```

### 데이터 저장

Beat 시나리오는 다음과 같이 저장됩니다:

```kotlin
ScenarioEntity(
    id = "scenario_123",
    title = "공원에서의 첫 만남",
    description = "{
        \"type\": \"beat\",
        \"description\": \"철수와 영희의 운명적인 만남\",
        \"beats\": \"[{Beat JSON}]\",
        \"characters\": \"[{CharacterInfo JSON}]\"
    }",
    moduleCount = 3,
    estimatedDuration = 12
)
```

재생 시:

1. description에서 JSON 파싱
2. Beat 리스트 복원
3. `BeatConverter.beatsToTheaterScript()` 변환
4. `StageView`에서 재생

---

## 실전 활용 예시

### 예시 1: 간단한 만남 장면

```
시나리오: "카페에서�� 재회"

등장인물:
  - 지수 (여)
  - 민호 (남)

Beat 타임라인:
  1. 등장 (지수) - 3초
     → 지수가 카페에 들어와 둘러본다
  
  2. 발견 (민호 독백) - 2초
     → 민호: "저기... 지수 아니야?"
  
  3. 첫 만남 - 4초
     → 두 사람이 서로를 발견하고 반가워함
  
  4. 어색한 침묵 - 3초
     → 무슨 말을 해야 할지 고민
  
  5. 대화 시작 (커스텀) - 5초
     → 지수: "여기 자주 와?"
     → 민호: "응, 커피가 맛있어서..."

총 17초
```

### 예시 2: 극적인 갈등 장면

```
시나리오: "오해와 진실"

등장인물:
  - 서연 (여)
  - 준호 (남)

Beat 타임라인:
  1. 대립 - 5초
     → 서연이 화내며 다가감
     → 준호가 당황하여 물러남
  
  2. 물러서기 (준호) - 3초
     → 준호: "잠깐만... 들어봐..."
  
  3. 커스텀: 진실 고백 - 6초
     → 준호가 용기 내어 진실을 말함
     → 준호: "사실은... 내가 아니었어"
  
  4. 커스텀: 깨달음 - 4초
     → 서연의 표정이 변함 (분노 → 혼란)
  
  5. 화해 - 5초
     → 서연: "미안해..."
     → 두 사람이 가까워짐

총 23초
```

---

## 장점 요약

### ✅ 연극 제작자에게

- **빠른 블로킹 테스트**: 배우 동선을 시각적으로 확인
- **타이밍 실험**: Beat별 시간 조절로 템포 조율
- **커뮤니케이션**: 팀원들과 명확한 의도 공유

### ✅ 초보자에게

- **쉬운 접근**: "첫 만남", "갈등" 같은 익숙한 개념
- **빠른 제작**: 템플릿 조합만으로 시나리오 완성
- **학습 효과**: Beat 구조를 이해하며 연극 감각 향상

### ✅ 개발자에게

- **명확한 구조**: 레이어드 아키텍처로 관리 용이
- **확장 가능**: 새로운 레이어나 템플릿 추가 쉬움
- **재사용성**: Beat를 다른 프로젝트에도 활용 가능

---

## 다음 단계

### Phase 1 (현재)

- ✅ Beat 데이터 모델
- ✅ Beat → TheaterScript 변환
- ✅ 기본 템플릿 (7종)
- ✅ BeatCreatorScreen UI

### Phase 2 (추후)

- [ ] Beat 에디터 (세부 커스터마이징)
- [ ] 더 많은 템플릿 (20+)
- [ ] 템플릿 마켓플레이스
- [ ] 커뮤니티 공유 기능

### Phase 3 (미래)

- [ ] 드래그 앤 드롭 타임라인
- [ ] 실시간 미리보기
- [ ] AI 기반 Beat 추천
- [ ] 영상 녹화 및 내보내기

---

## 참고 자료

### 연극 이론

- Konstantin Stanislavski - "An Actor Prepares"
- Robert Cohen - "Theatre Brief"

### 구현 파일

- `app/src/main/java/com/example/cutestage/stage/beat/BeatModels.kt`
- `app/src/main/java/com/example/cutestage/stage/beat/BeatConverter.kt`
- `app/src/main/java/com/example/cutestage/stage/beat/BeatTemplates.kt`
- `app/src/main/java/com/example/cutestage/ui/creator/BeatCreatorScreen.kt`
- `app/src/main/java/com/example/cutestage/ui/creator/BeatCreatorViewModel.kt`

---

**CuteStage Beat System v1.0**
연극 제작의 새로운 패러다임 🎭
